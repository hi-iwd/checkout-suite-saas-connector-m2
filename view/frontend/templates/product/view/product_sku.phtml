<?php
$viewModel = $block->getData('view_model');
if ($viewModel->isEnable()):
    ?>
    <script>
        window.currentProductSku = <?= /* @noEscape */ json_encode($viewModel->getProductData()) ?>;
        window.subscription_btn = <?= /* @noEscape */ json_encode($viewModel->getBtnConfiguration()) ?>
    </script>
<?php
endif;
?>
